List of TODO things

Gnome-CD: 
 * Needs ported to other Unix systems.
 * Display needs a nice design
 * Need graphics.
 * Track editor
 * Preferences
 * Needs to use CDDBSlave.
 * Needs a CORBA interface so it can be run as a guiless CD player 
   (possibly by the CD player applet)

GMix:
 * Ported to other Unix systems
 * Needs to be finished to use the abstraction objects.
 * Needs ported to GConf
 * Needs a CORBA interface.

GRecord:
 * Someone who knows how this is supposed to work needs to check and see if it 
   does :)

CDDBSlave:
 * Needs to use GConf more (notifications and stuff).

VU-Meter:
 * Done.

Misc:
 * Documentation.

Multimedia applets need moved in if thats what we're doing to the applets.
A BonoboControl like the Delphi MediaControl thing.
A BonoboControl for setting the volume like in Windows.
